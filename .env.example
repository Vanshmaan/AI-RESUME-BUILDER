# Server — copy to server/.env (do not commit real values)
PORT=
MONGODB_URI=
JWT_SECRET=
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=
IMAGEKIT_PRIVATE_KEY=

# Client — copy to client/.env
VITE_BASE_URL=
